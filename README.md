# SLAM Tracepoint Analysis

A ROS2 package providing scripts to process LTTng traces containing `slam_tracepoint_provider` instrumentation points

## Usage 

```
 ros2 run slam_tracepoint_analysis process  <lttng-traces-path> <outputfile>.png
```